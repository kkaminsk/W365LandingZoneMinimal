param lawId string
param resourceIds array = []
param namePrefix string = 'diag-'

// Placeholder module: implement per-resource diagnostic settings later.
output note string = 'Add resource diagnostics in a later step.'
